"""
processing/transcription.py — Phase 5.

Offline speech-to-text with faster-whisper. Loads the model named by
MeetingSettings.whisper_model on CPU (configurable) and returns timestamped
segments: [{start, end, text}]. No cloud speech API.

Stub only in Phase 1.
"""
